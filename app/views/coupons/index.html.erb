<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <% @coupons.each do |coupon| %>
      <%= coupon.store %>
      <%end%>

      <div>
        <% @coupons.each do |coupon| %>
          <div><%= link_to coupon.coupon_code, coupon_path(coupon) %></div>
        <% end %>
      </div>
  </body>
</html>
